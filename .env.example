# Techem Prediction Engine — Environment Variables
# Copy this file to .env and fill in your values.

# ── Gemini LLM (required for /chat, optional for /recommendations, /leaks, /target) ──
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
LLM_MAX_TOOL_ITERATIONS=6
